document.addEventListener('DOMContentLoaded', function() {
    const modules = document.getElementsByClassName('blog-listing-sidebar-module');
    for (let i = 0; i < modules.length; i++) {
        const module = modules[i];
    }
});