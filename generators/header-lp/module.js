document.addEventListener('DOMContentLoaded', function() {
    const modules = document.getElementsByClassName('header-lp-module');
    for (let i = 0; i < modules.length; i++) {
        const module = modules[i];
    }
});